// Type definitions for TweetYoink extension

/**
 * Future: User configuration for backend connectivity
 * Will be implemented when backend integration is added
 */
export interface ExtensionConfig {
  backendUrl?: string;
  apiKey?: string;
  // Additional fields to be added in future features
}

// Additional types will be added as features are implemented
